# Acknowledgements

- **AbstractObserver**: the Entity app began as a second page of the observer
  and became its own package so each app has one purpose.
- **AbstractUIC**: the shared UI kit (`@abstractframework/ui-kit`,
  `@abstractframework/panel-chat`), the sign-in proxy
  (`@abstractframework/app-server`) and the cognition-wave scorer vendored in
  `src/vendor/cognition/`.
- **AbstractGateway**, **AbstractRuntime** and **AbstractMemory**: the services
  that host entities, run their lifecycle and produce the replay stream this app
  renders.
- Built with [React](https://react.dev), [Vite](https://vite.dev),
  [TypeScript](https://www.typescriptlang.org) and [Vitest](https://vitest.dev).
