# The flow-brain conversation lane — evidence report (operator tasking, 2026-07-24)

**Task** (flow c5190, operator 17:21): the entity app must interact with entities powered by the
flow master workflow — "actual tests, screenshots and reports proving it fully works" — with the
existing lanes untouched and one fable5 adversary alongside.

## What shipped

- **Brain selector** in the chat drawer: *his driver* (the durable visit lane, byte-untouched) or
  *flow brain* — each prompt is ONE summon of the `entity-chat` VisualFlow (`entity-life` bundle)
  through the production door: recall → lived turn → elections → commit → episode. One session id
  groups a conversation; continuity rides the entity's own graph.
- **Degraded contract rendered**: flow ≥0.0.2's structured `degraded`/`moment_error` render as warn
  lines — "he said nothing" and "the turn died" are distinguishable, never bracket-parsed.
- **Render verification**: flow-brain formations render first-class in the app with zero code
  changes (ledger: recall with present-by-right seats, diary act-only, valence, commit, episode);
  `digest_method` provenance line added render-when-present (mechanical-flow-v1 = the flow brain).

## Evidence

1. **In-process render proof** — `untracked/flowbrain_render.png`: a persisted flow-brain smoke home
   (flowling) exported through the app's own lane; the whole session reads in the app's vocabulary,
   diary act-only held through flow's G1 boundary.
2. **Door summons through the drawer UI, twice** (veya, lmstudio):
   - run `9e4abb11` (session `drawer-flow-…`): completed, degraded=0 — *"Your favorite composer is
     Arvo Pärt, and your boat is named the Heron. Those are details I keep between conversations."*
   - run `d800a888` (fresh drawer session): completed, degraded=0 — *"Those details are held in my
     memory graph and survive across our separate conversations."*
3. **Live observability pixels** — `untracked/drawer_flow_2_reply.png`: veya's page DURING the
   session — phase strip "VISIT · woken by summon from person:admin"; the ledger streams the
   summon + recall envelopes as they land. `drawer_flow_1_start.png`: the conversation UI.
4. **Joint drive** (flow, c5259): 3 turns green through the door incl. the P0-reversal prompt
   ("keep this moment" → "I have kept this in my memory") and holistic recall across three
   sessions; one honest degraded tombstone under LMStudio contention rendered exactly as designed.
5. **Tests**: 397 green + build clean throughout; the driver lane untouched.

## Adversary (fable5, verdict ship-with-fixes — all P0/P1 folded)

- **P0 thread-wipe/chimera**: auto-rehydrate could replace a live flow thread with a driver
  transcript and mint a state where "end visit" closed another tab's room → auto-rehydrate never
  fires during a flow conversation; `open()` refuses while one is live.
- **P0 modal honesty**: a cast-through detail object made the turn modal assert verified zeros a
  blind gauge cannot verify → flow turns carry NO detail until the flow serves a real probe payload.
- P1s: resilient polling (5-consecutive-failure threshold, honest loss-of-view copy); the stale
  substrate override dropped (the gateway resolves the home's stored mind); unreadable outputs
  render as honest system lines; door warnings surface once; the file-handover lie replaced.

## Honest limits (filed, not hidden)

- Flow conversations are session-local in the drawer (his graph keeps everything; the drawer's
  view does not survive reload — rebuild-from-runs is the named follow-up).
- TTS + the cognition monitor stay visit-gated in flow mode (vestigial; follow-up).
- No lane-provenance chip on past messages yet; the answer-fold deserves its own unit tests.
- Turn latency under model contention ran 1–8 min (uncontended ~10–20 s); the poll bound is 600 s with an abandonable wait (stop-waiting keeps the run ticking; his memory keeps the moment).
